name: make-lint
on: push
jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: Install
        # Install project
        run: make install
      - name: Run linter
        shell: bash
        run: make lint